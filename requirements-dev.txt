# Dependencias de desarrollo y medición de cobertura.
# Instalación:  pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest>=8.0.0
pytest-cov>=5.0.0
coverage[toml]>=7.4.0
